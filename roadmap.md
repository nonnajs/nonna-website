# Roadmap

- [ ] React playground page using the real `@nonnajs/di` + `@nonnajs/react` packages.
      Blocked: every published version is less than 24h old, so the installer
      refuses them (minimum release age). Retry after Sat 19 Sep 2026 ~18:00 UTC,
      then install pinned versions (`@nonnajs/di@1.1.0`, `@nonnajs/react@2.0.0`
      or newer) and build `/playground` with live editable providers + output.
