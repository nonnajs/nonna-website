# Nonna favicon and search visibility

## Changes
- Replace the existing browser favicon with the uploaded Nonna arch icon while leaving the current on-page wordmark unchanged.
- Strengthen each page’s unique title and description around JavaScript and TypeScript dependency injection searches.
- Expand the comparison page metadata to position Nonna as an alternative to TypeDI, InversifyJS, tsyringe, NestJS DI, Awilix, and BottleJS without unsupported ranking claims.
- Add concise structured data for the Nonna open-source software project and its author, Manuel Santos.
- Add a public `agents.txt` that identifies the site, documentation pages, canonical GitHub/npm locations, supported runtimes/frameworks, and crawler access guidance.
- Preserve the current crawler access rules and avoid adding a sitemap until the site has a public URL.

## Verification
- Confirm the new favicon is served and referenced correctly.
- Check every public page has unique title, description, Open Graph, Twitter card, canonical, and URL metadata.
- Confirm `agents.txt` is publicly reachable and that the visible Nonna logo has not changed.
